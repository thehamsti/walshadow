[package]
name = "walshadow-bench"
version = "0.1.0"
edition = "2024"
description = "Replication-latency benchmarks (Postgres → ClickHouse / PG standby), engine + CLIs"
license = "AGPL-3.0-only"

# The shared engine reuses walshadow's authenticated Snowflake SQL transport.
[lib]
name = "walshadow_bench"
path = "src/lib.rs"

# Local docker-compose stack (endpoints default to localhost).
[[bin]]
name = "walshadow-local-bench"
path = "src/bin/local_bench.rs"

# EC2 deployment (endpoints resolved from the harness state.env files).
[[bin]]
name = "walshadow-ec2-bench"
path = "src/bin/ec2_bench.rs"

[[bin]]
name = "walshadow-bench-plot"
path = "src/bin/plot.rs"

[[bin]]
name = "walshadow-snowflake-qualify"
path = "src/bin/snowflake_qualify.rs"

[dependencies]
walshadow = { path = ".." }
uuid = { version = "1", features = ["v4"] }
ab_glyph = "0.2.32"
anyhow = "1"
async-trait = "0.1"
clap = { version = "4", features = ["derive"] }
csv = "1.4"
dejavu = "2.37"
serde_json = "1"
tiny-skia = "0.12"
tokio = { version = "1", features = ["rt", "rt-multi-thread", "macros", "time", "net", "sync", "io-util", "process"] }
tokio-postgres = "0.7"
url = "2"

[dev-dependencies]
tempfile = "3"
